each shaddow is an object that decides if it should be rendered and how

events to put self in queue 

render connected to enter_frame


render only takes bitmap data

every shape is a subclas of renderable

tool listens to a "selectedShape" event

than "changeParam"


-------------------------------

